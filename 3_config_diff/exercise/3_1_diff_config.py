# Importing a new library!
from genie.utils.diff import Diff
import json
import os

# Step 0: Opening the two files
config_modified = "to be replaced"
config_original = "to be replaced"

# Step 1: Printting the differences
print ("the differences")